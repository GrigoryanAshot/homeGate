Drop your sliding-gate art here (PNG):

  frame.png  — posts / framing (stays still)
  door.png   — the moving leaf (slides open/closed)

Then open the app, pick “Sliding” for that gate.
If sizes look off, tweak NATIVE in SlideDoorVisualizer.tsx.
