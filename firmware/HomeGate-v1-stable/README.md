# HomeGate firmware — frozen v1 (pre remote Wi‑Fi / BLE)

Snapshot of the working SoftAP + MQTT build **before** in-app Wi‑Fi change and BLE rescue.

- Flash this sketch if you need to roll back.
- Active development: `../HomeGate`
